<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liste des Apprenants - Job Dating YouCode</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#667eea',
                        secondary: '#764ba2',
                    }
                }
            }
        }
    </script>
</head>
<body class="bg-gradient-to-br from-primary to-secondary min-h-screen p-4 md:p-6">
    <div class="max-w-7xl mx-auto">
        <!-- Header -->
        <div class="bg-white rounded-2xl shadow-2xl p-6 mb-6 flex flex-col md:flex-row justify-between items-center gap-4">
            <h1 class="text-2xl md:text-3xl font-bold text-primary flex items-center gap-3">
                <span class="text-3xl">👥</span>
                Liste des Apprenants
            </h1>
            <a href="/admin/dashboard" class="bg-gray-500 hover:bg-gray-600 text-white px-6 py-2 rounded-lg flex items-center gap-2 transition-all duration-300 hover:shadow-lg">
                <span>🔙</span>
                Retour au Dashboard
            </a>
        </div>

        <!-- Content -->
        <div class="bg-white rounded-2xl shadow-2xl p-6">
            {% if students is empty %}
                <div class="text-center py-12">
                    <div class="text-6xl mb-4">👥</div>
                    <h3 class="text-xl font-semibold text-gray-700 mb-2">Aucun apprenant trouvé</h3>
                    <p class="text-gray-500">Aucun apprenant n'est inscrit pour le moment.</p>
                    <a href="/admin/dashboard" class="bg-primary hover:bg-opacity-90 text-white px-6 py-2 rounded-lg inline-flex items-center gap-2 transition-all duration-300 hover:shadow-lg mt-4">
                        <span class="flex items-center gap-2">
                            <span>🎯</span>
                            Retour au Dashboard
                        </span>
                    </a>
                </div>
            {% else %}
                <div class="mb-4">
                    <p class="text-gray-600">
                        <span class="font-semibold">{{ students|length }}</span> apprenant(s) inscrit(s)
                    </p>
                </div>

                <div class="overflow-x-auto">
                    <table class="w-full">
                        <thead>
                            <tr class="border-b border-gray-200">
                                <th class="text-left py-3 px-4 font-semibold text-gray-700">Nom</th>
                                <th class="text-left py-3 px-4 font-semibold text-gray-700">Email</th>
                                <th class="text-left py-3 px-4 font-semibold text-gray-700">Promotion</th>
                                <th class="text-left py-3 px-4 font-semibold text-gray-700">Spécialisation</th>
                                <th class="text-left py-3 px-4 font-semibold text-gray-700">Date d'inscription</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for student in students %}
                                <tr class="border-b border-gray-100 hover:bg-gray-50 transition-colors">
                                    <td class="py-3 px-4">
                                        <div class="font-medium text-gray-900">{{ student.name }}</div>
                                    </td>
                                    <td class="py-3 px-4">
                                        <span class="text-gray-600">{{ student.email }}</span>
                                    </td>
                                    <td class="py-3 px-4">
                                        <span class="text-gray-600">{{ student.promotion ?? 'Non spécifiée' }}</span>
                                    </td>
                                    <td class="py-3 px-4">
                                        <span class="text-gray-600">{{ student.specialisation ?? 'Non spécifiée' }}</span>
                                    </td>
                                    <td class="py-3 px-4">
                                        <span class="text-gray-600">
                                            {{ student.created_at ? student.created_at|date('d/m/Y H:i') : 'N/A' }}
                                        </span>
                                    </td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            {% endif %}
        </div>
    </div>

    <script>
        // Fonction pour confirmer les actions
        function confirmAction(message) {
            return confirm(message);
        }
    </script>
</body>
</html>