<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard Administrateur - Job Dating YouCode</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#667eea',
                        secondary: '#764ba2',
                    }
                }
            }
        }
    </script>
</head>
<body class="bg-gradient-to-br from-primary to-secondary min-h-screen p-4 md:p-6">
    <div class="max-w-7xl mx-auto">
        <!-- Header -->
        <div class="bg-white rounded-2xl shadow-2xl p-6 mb-6 flex flex-col md:flex-row justify-between items-center gap-4">
            <h1 class="text-2xl md:text-3xl font-bold text-primary flex items-center gap-3">
                <span class="text-3xl">🎯</span>
                Dashboard Administrateur - Job Dating YouCode
            </h1>
            <div class="flex items-center gap-4">
                <span class="text-gray-600">Bienvenue, <strong class="text-gray-900">Admin</strong></span>
                <button onclick="logout()" class="bg-red-500 hover:bg-red-600 text-white px-6 py-2 rounded-lg flex items-center gap-2 transition-all duration-300 hover:shadow-lg">
                    <span>🚪</span>
                    Déconnexion
                </button>
            </div>
        </div>

        <!-- Statistics Cards -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-6">
            <!-- Active Announcements -->
            <div class="bg-white rounded-2xl shadow-xl p-6 hover:transform hover:-translate-y-2 transition-all duration-300">
                <div class="flex items-center justify-between mb-4">
                    <div class="w-14 h-14 bg-gradient-to-br from-primary to-secondary rounded-xl flex items-center justify-center text-3xl">
                        💼
                    </div>
                </div>
                <h3 class="text-gray-600 text-sm font-medium mb-2">Annonces Actives</h3>
                <p class="text-4xl font-bold text-gray-900">{{ stats.announcements }}</p>
            </div>

            <!-- Archived Announcements -->
            <div class="bg-white rounded-2xl shadow-xl p-6 hover:transform hover:-translate-y-2 transition-all duration-300">
                <div class="flex items-center justify-between mb-4">
                    <div class="w-14 h-14 bg-gradient-to-br from-orange-400 to-red-500 rounded-xl flex items-center justify-center text-3xl">
                        📦
                    </div>
                </div>
                <h3 class="text-gray-600 text-sm font-medium mb-2">Annonces Archivées</h3>
                <p class="text-4xl font-bold text-gray-900">{{ stats.archived }}</p>
            </div>

            <!-- Companies -->
            <div class="bg-white rounded-2xl shadow-xl p-6 hover:transform hover:-translate-y-2 transition-all duration-300">
                <div class="flex items-center justify-between mb-4">
                    <div class="w-14 h-14 bg-gradient-to-br from-green-500 to-green-600 rounded-xl flex items-center justify-center text-3xl">
                        🏢
                    </div>
                </div>
                <h3 class="text-gray-600 text-sm font-medium mb-2">Entreprises Partenaires</h3>
                <p class="text-4xl font-bold text-gray-900">{{ stats.companies }}</p>
            </div>

            <!-- Students -->
            <div class="bg-white rounded-2xl shadow-xl p-6 hover:transform hover:-translate-y-2 transition-all duration-300">
                <div class="flex items-center justify-between mb-4">
                    <div class="w-14 h-14 bg-gradient-to-br from-blue-500 to-blue-600 rounded-xl flex items-center justify-center text-3xl">
                        🎓
                    </div>
                </div>
                <h3 class="text-gray-600 text-sm font-medium mb-2">Apprenants Inscrits</h3>
                <p class="text-4xl font-bold text-gray-900">{{ stats.students }}</p>
            </div>
        </div>

        <!-- Content Grid -->
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
            <!-- Recent Announcements -->
            <div class="lg:col-span-2 bg-white rounded-2xl shadow-xl p-6">
                <h2 class="text-2xl font-bold text-gray-900 mb-6 pb-4 border-b-2 border-gray-100">
                    Dernières Annonces
                </h2>

                {% for announcement in recentAnnouncements %}
                <div class="bg-gray-50 rounded-xl p-4 mb-4 hover:bg-gray-100 transition-all duration-300 hover:transform hover:translate-x-2">
                    <h4 class="text-lg font-semibold text-gray-900 mb-3">{{ announcement.title }}</h4>
                    <div class="flex flex-wrap gap-3 text-sm text-gray-600 mb-3">
                        <span class="flex items-center gap-1">
                            <span>🏢</span> {{ announcement.company_name }}
                        </span>
                        <span class="flex items-center gap-1">
                            <span>�</span> {{ announcement.company_location }}
                        </span>
                        <span class="flex items-center gap-1">
                            <span>�</span> Il y a {{ announcement.created_at|date("j") }} jours
                        </span>
                        <span class="bg-blue-100 text-blue-800 px-3 py-1 rounded-full text-xs font-medium">
                            {{ announcement.contract_type }}
                        </span>
                    </div>
                    <div class="flex flex-wrap gap-2">
                        <a href="/announcements/show/{{ announcement.id }}" class="bg-primary hover:bg-opacity-90 text-white px-4 py-2 rounded-lg text-sm flex items-center gap-2 transition-all duration-300 hover:shadow-lg">
                            <span>👁️</span> Voir
                        </a>
                        <a href="/announcements/edit/{{ announcement.id }}" class="bg-primary hover:bg-opacity-90 text-white px-4 py-2 rounded-lg text-sm flex items-center gap-2 transition-all duration-300 hover:shadow-lg">
                            <span>✏️</span> Modifier
                        </a>
                        <form action="/announcements/archive/{{ announcement.id }}" method="POST" style="display: inline;" onsubmit="return confirm('Êtes-vous sûr de vouloir archiver cette annonce?')">
                            <button type="submit" class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-lg text-sm flex items-center gap-2 transition-all duration-300 hover:shadow-lg">
                                <span>📦</span> Archiver
                            </button>
                        </form>
                        <form action="/announcements/delete/{{ announcement.id }}" method="POST" style="display: inline;" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer définitivement cette annonce? Cette action est irréversible!')">
                            <button type="submit" class="bg-dark hover:bg-gray-700 text-white px-4 py-2 rounded-lg text-sm flex items-center gap-2 transition-all duration-300 hover:shadow-lg">
                                <span>🗑️</span> Supprimer
                            </button>
                        </form>
                    </div>
                </div>
                {% else %}
                <p class="text-gray-500 text-center py-8">Aucune annonce récente trouvée</p>
                {% endfor %}
            </div>

            <!-- Quick Actions -->
            <div class="bg-white rounded-2xl shadow-xl p-6">
                <h2 class="text-2xl font-bold text-gray-900 mb-6 pb-4 border-b-2 border-gray-100">
                    Actions Rapides
                </h2>

                <div class="space-y-3">
                    <a href="/announcements/create" class="flex items-center gap-4 p-4 bg-gradient-to-r from-gray-50 to-gray-100 rounded-xl hover:from-primary hover:to-secondary hover:text-white transition-all duration-300 hover:transform hover:translate-x-2 group">
                        <div class="w-12 h-12 bg-white rounded-lg flex items-center justify-center text-2xl group-hover:scale-110 transition-transform">
                            ➕
                        </div>
                        <span class="font-medium">Créer une Annonce</span>
                    </a>

                    <a href="#" onclick="addCompany(event)" class="flex items-center gap-4 p-4 bg-gradient-to-r from-gray-50 to-gray-100 rounded-xl hover:from-primary hover:to-secondary hover:text-white transition-all duration-300 hover:transform hover:translate-x-2 group">
                        <div class="w-12 h-12 bg-white rounded-lg flex items-center justify-center text-2xl group-hover:scale-110 transition-transform">
                            🏢
                        </div>
                        <span class="font-medium">Ajouter une Entreprise</span>
                    </a>

                    <a href="#" onclick="viewArchived(event)" class="flex items-center gap-4 p-4 bg-gradient-to-r from-gray-50 to-gray-100 rounded-xl hover:from-primary hover:to-secondary hover:text-white transition-all duration-300 hover:transform hover:translate-x-2 group">
                        <div class="w-12 h-12 bg-white rounded-lg flex items-center justify-center text-2xl group-hover:scale-110 transition-transform">
                            📦
                        </div>
                        <span class="font-medium">Voir les Archives</span>
                    </a>

                    <a href="#" onclick="viewCompanies(event)" class="flex items-center gap-4 p-4 bg-gradient-to-r from-gray-50 to-gray-100 rounded-xl hover:from-primary hover:to-secondary hover:text-white transition-all duration-300 hover:transform hover:translate-x-2 group">
                        <div class="w-12 h-12 bg-white rounded-lg flex items-center justify-center text-2xl group-hover:scale-110 transition-transform">
                            💼
                        </div>
                        <span class="font-medium">Gérer les Entreprises</span>
                    </a>

                    <a href="#" onclick="viewStudents(event)" class="flex items-center gap-4 p-4 bg-gradient-to-r from-gray-50 to-gray-100 rounded-xl hover:from-primary hover:to-secondary hover:text-white transition-all duration-300 hover:transform hover:translate-x-2 group">
                        <div class="w-12 h-12 bg-white rounded-lg flex items-center justify-center text-2xl group-hover:scale-110 transition-transform">
                            👥
                        </div>
                        <span class="font-medium">Voir les Apprenants</span>
                    </a>
                </div>
            </div>
        </div>
    </div>

    <script>
        function logout() {
            if (confirm('Êtes-vous sûr de vouloir vous déconnecter ?')) {
                window.location.href = '/logout';
            }
        }

        function archiveAnnouncement() {
            if (confirm('Êtes-vous sûr de vouloir archiver cette annonce ?')) {
                alert('Annonce archivée avec succès');
            }
        }

        function createAnnouncement(e) {
            e.preventDefault();
            window.location.href = '/announcements/create';
        }
        function addCompany(e) {
            e.preventDefault();
            window.location.href = '/companies/create';
        }

        function viewArchived(e) {
            e.preventDefault();
            window.location.href = '/announcements/archived';
        }

        function viewCompanies(e) {
            e.preventDefault();
            window.location.href = '/companies';
        }

        
    </script>
</body>
</html>