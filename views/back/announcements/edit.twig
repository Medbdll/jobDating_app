<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier {{ announcement.title }} - Job Dating YouCode</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#667eea',
                        secondary: '#764ba2',
                    },
                    animation: {
                        'fade-in': 'fadeIn 0.5s ease-in-out',
                        'slide-up': 'slideUp 0.6s ease-out',
                        'scale-in': 'scaleIn 0.4s ease-out',
                    }
                }
            }
        }
    </script>
    <style>
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        @keyframes slideUp {
            from { 
                opacity: 0;
                transform: translateY(20px);
            }
            to { 
                opacity: 1;
                transform: translateY(0);
            }
        }
        @keyframes scaleIn {
            from { 
                opacity: 0;
                transform: scale(0.9);
            }
            to { 
                opacity: 1;
                transform: scale(1);
            }
        }
        .animate-fade-in { animation: fadeIn 0.5s ease-in-out; }
        .animate-slide-up { animation: slideUp 0.6s ease-out; }
        .animate-scale-in { animation: scaleIn 0.4s ease-out; }
        .glass-effect {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
        }
        .form-input {
            transition: all 0.3s ease;
        }
        .form-input:focus {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(102, 126, 234, 0.2);
        }
        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            transition: all 0.3s ease;
        }
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
        }
    </style>
</head>
<body class="bg-gradient-to-br from-primary via-purple-600 to-secondary min-h-screen p-4 md:p-8">
    <!-- Background Pattern -->
    <div class="fixed inset-0 opacity-10">
        <div class="absolute inset-0" style="background-image: url('data:image/svg+xml,%3Csvg width="60" height="60" viewBox="0 0 60 60" xmlns="http://www.w3.org/2000/svg"%3E%3Cg fill="none" fill-rule="evenodd"%3E%3Cg fill="%23ffffff" fill-opacity="0.4"%3E%3Ccircle cx="30" cy="30" r="4"/%3E%3C/g%3E%3C/g%3E%3C/svg%3E');"></div>
    </div>

    <div class="max-w-5xl mx-auto relative z-10">
        <!-- Header -->
        <div class="glass-effect rounded-3xl shadow-2xl p-8 mb-8 animate-fade-in border border-white/20">
            <div class="flex flex-col md:flex-row justify-between items-center gap-6">
                <div class="flex items-center gap-4">
                    <div class="w-16 h-16 bg-gradient-to-br from-white to-gray-100 rounded-2xl flex items-center justify-center shadow-lg">
                        <i class="fas fa-edit text-2xl text-primary"></i>
                    </div>
                    <div>
                        <h1 class="text-3xl md:text-4xl font-bold text-black">Modifier l'Annonce</h1>
                        <p class="text-black/80 mt-1">Mettez à jour les informations de l'annonce</p>
                    </div>
                </div>
                <a href="/dashboard" class="group bg-white/20 hover:bg-white/30 text-black px-6 py-3 rounded-xl flex items-center gap-3 transition-all duration-300 backdrop-blur-sm border border-white/30">
                    <i class="fas fa-arrow-left group-hover:-translate-x-1 transition-transform"></i>
                    <span class="font-medium">Retour</span>
                </a>
            </div>
        </div>

        <!-- Flash Messages -->
        {% if flash.success %}
        <div class="bg-green-500/90 backdrop-blur-sm text-black px-6 py-4 rounded-2xl mb-6 animate-slide-up flex items-center gap-3 border border-green-400/30">
            <i class="fas fa-check-circle text-xl"></i>
            <span>{{ flash.success }}</span>
        </div>
        {% endif %}

        {% if flash.error %}
        <div class="bg-red-500/90 backdrop-blur-sm text-black px-6 py-4 rounded-2xl mb-6 animate-slide-up flex items-center gap-3 border border-red-400/30">
            <i class="fas fa-exclamation-circle text-xl"></i>
            <span>{{ flash.error }}</span>
        </div>
        {% endif %}

        <!-- Form -->
        <div class="glass-effect rounded-3xl shadow-2xl p-8 md:p-10 animate-slide-up border border-white/20">
            <form action="/announcements/update/{{ announcement.id }}" method="POST" class="space-y-8">
                <!-- Company Selection -->
                <div class="group">
                    <label class="block text-black font-semibold mb-3 flex items-center gap-2">
                        <i class="fas fa-building text-primary"></i>
                        Entreprise *
                    </label>
                    <div class="relative">
                        <select name="company_id" required 
                                class="w-full px-6 py-4 bg-white/90 backdrop-blur-sm border border-white/30 rounded-2xl focus:ring-4 focus:ring-primary/30 focus:border-primary transition-all duration-300 text-gray-800 appearance-none cursor-pointer hover:bg-white form-input">
                            <option value="">Sélectionner une entreprise</option>
                            {% for company in companies %}
                            <option value="{{ company.id }}" {% if company.id == announcement.company_id %}selected{% endif %} class="text-gray-800">{{ company.name }} - {{ company.location }}</option>
                            {% endfor %}
                        </select>
                        <div class="absolute right-4 top-1/2 -translate-y-1/2 pointer-events-none">
                            <i class="fas fa-chevron-down text-gray-600"></i>
                        </div>
                    </div>
                </div>

                <!-- Title -->
                <div class="group">
                    <label class="block text-black font-semibold mb-3 flex items-center gap-2">
                        <i class="fas fa-heading text-primary"></i>
                        Titre de l'annonce *
                    </label>
                    <input type="text" name="title" required value="{{ announcement.title }}"
                           class="w-full px-6 py-4 bg-white/90 backdrop-blur-sm border border-white/30 rounded-2xl focus:ring-4 focus:ring-primary/30 focus:border-primary transition-all duration-300 text-gray-800 placeholder-gray-500 hover:bg-white form-input"
                           placeholder="Ex: Développeur Full Stack Senior">
                </div>

                <!-- Contract Type and Location -->
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div class="group">
                        <label class="block text-black font-semibold mb-3 flex items-center gap-2">
                            <i class="fas fa-file-contract text-primary"></i>
                            Type de contrat *
                        </label>
                        <select name="contract_type" required 
                                class="w-full px-6 py-4 bg-white/90 backdrop-blur-sm border border-white/30 rounded-2xl focus:ring-4 focus:ring-primary/30 focus:border-primary transition-all duration-300 text-gray-800 appearance-none cursor-pointer hover:bg-white form-input">
                            <option value="">Sélectionner</option>
                            <option value="CDI" {% if announcement.contract_type == 'CDI' %}selected{% endif %}>CDI</option>
                            <option value="CDD" {% if announcement.contract_type == 'CDD' %}selected{% endif %}>CDD</option>
                            <option value="Freelance" {% if announcement.contract_type == 'Freelance' %}selected{% endif %}>Freelance</option>
                            <option value="Stage" {% if announcement.contract_type == 'Stage' %}selected{% endif %}>Stage</option>
                            <option value="Alternance" {% if announcement.contract_type == 'Alternance' %}selected{% endif %}>Alternance</option>
                        </select>
                    </div>

                    <div class="group">
                        <label class="block text-black font-semibold mb-3 flex items-center gap-2">
                            <i class="fas fa-map-marker-alt text-primary"></i>
                            Localisation *
                        </label>
                        <input type="text" name="location" required value="{{ announcement.location }}"
                               class="w-full px-6 py-4 bg-white/90 backdrop-blur-sm border border-white/30 rounded-2xl focus:ring-4 focus:ring-primary/30 focus:border-primary transition-all duration-300 text-gray-800 placeholder-gray-500 hover:bg-white form-input"
                               placeholder="Ex: Casablanca">
                    </div>
                </div>

                <!-- Description -->
                <div class="group">
                    <label class="block text-black font-semibold mb-3 flex items-center gap-2">
                        <i class="fas fa-align-left text-primary"></i>
                        Description *
                    </label>
                    <textarea name="description" required rows="6"
                              class="w-full px-6 py-4 bg-white/90 backdrop-blur-sm border border-white/30 rounded-2xl focus:ring-4 focus:ring-primary/30 focus:border-primary transition-all duration-300 text-gray-800 placeholder-gray-500 resize-none hover:bg-white form-input"
                              placeholder="Décrivez le poste, les missions quotidiennes, les avantages, l'équipe...">{{ announcement.description }}</textarea>
                </div>

                <!-- Skills -->
                <div class="group">
                    <label class="block text-black font-semibold mb-3 flex items-center gap-2">
                        <i class="fas fa-tools text-primary"></i>
                        Compétences requises
                    </label>
                    <input type="text" name="skills" value="{{ announcement.skills }}"
                           class="w-full px-6 py-4 bg-white/90 backdrop-blur-sm border border-white/30 rounded-2xl focus:ring-4 focus:ring-primary/30 focus:border-primary transition-all duration-300 text-gray-800 placeholder-gray-500 hover:bg-white form-input"
                           placeholder="Ex: PHP, JavaScript, React, MySQL (séparez par des virgules)">
                    <p class="text-black/70 text-sm mt-2">Indiquez les compétences techniques et qualités recherchées</p>
                </div>

                <!-- Submit Buttons -->
                <div class="flex flex-col md:flex-row justify-end gap-4 pt-8 border-t border-white/20">
                    <a href="/admin/dashboard" 
                       class="px-8 py-4 bg-white/20 hover:bg-white/30 text-black rounded-2xl transition-all duration-300 backdrop-blur-sm border border-white/30 text-center font-medium">
                        <i class="fas fa-times mr-2"></i>
                        Annuler
                    </a>
                    <button type="submit" 
                            class="px-8 py-4 btn-primary text-black rounded-2xl font-semibold flex items-center justify-center gap-3 group">
                        <i class="fas fa-save group-hover:translate-x-1 transition-transform"></i>
                        Mettre à jour l'annonce
                    </button>
                </div>
            </form>
        </div>

        <!-- Footer Info -->
        <div class="text-center mt-8 text-black/70 animate-fade-in">
            <p class="flex items-center justify-center gap-2">
                <i class="fas fa-info-circle"></i>
                Les champs marqués d'un astérisque (*) sont obligatoires
            </p>
        </div>
    </div>
</body>
</html>
