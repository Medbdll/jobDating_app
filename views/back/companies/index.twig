{% extends "layout.twig" %}

{% block content %}
<div class="bg-white rounded-2xl shadow-2xl p-6">
    <div class="flex justify-between items-center mb-6">
        <h2 class="text-2xl font-bold text-gray-900 flex items-center gap-3">
            <span class="text-3xl">�</span>
            Gérer les Entreprises
        </h2>
        <div class="flex gap-3">
            <a href="/companies/create" class="bg-primary hover:bg-opacity-90 text-black px-6 py-2 rounded-lg flex items-center gap-2 transition-all duration-300 hover:shadow-lg">
                <span>➕</span>
                Ajouter une Entreprise
            </a>
            <a href="/admin/dashboard" class="bg-gray-500 hover:bg-gray-600 text-white px-6 py-2 rounded-lg flex items-center gap-2 transition-all duration-300 hover:shadow-lg">
                <span>🔙</span>
                Retour au Dashboard
            </a>
        </div>
    </div>

    {% if companies is empty %}
        <div class="text-center py-12">
            <div class="text-6xl mb-4">🏢</div>
            <h3 class="text-xl font-semibold text-gray-700 mb-2">Aucune entreprise trouvée</h3>
            <p class="text-gray-500 mb-6">Commencez par ajouter votre première entreprise.</p>
            <a href="/companies/create" class="bg-primary hover:bg-opacity-90 text-white px-6 py-2 rounded-lg inline-flex items-center gap-2 transition-all duration-300 hover:shadow-lg">
                <span>➕</span>
                Ajouter une Entreprise
            </a>
        </div>
    {% else %}
        <div class="overflow-x-auto">
            <table class="w-full">
                <thead>
                    <tr class="border-b border-gray-200">
                        <th class="text-left py-3 px-4 font-semibold text-gray-700">Nom</th>
                        <th class="text-left py-3 px-4 font-semibold text-gray-700">Secteur</th>
                        <th class="text-left py-3 px-4 font-semibold text-gray-700">Localisation</th>
                        <th class="text-left py-3 px-4 font-semibold text-gray-700">Email</th>
                        <th class="text-left py-3 px-4 font-semibold text-gray-700">Téléphone</th>
                        <th class="text-left py-3 px-4 font-semibold text-gray-700">Date de création</th>
                        <th class="text-center py-3 px-4 font-semibold text-gray-700">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for company in companies %}
                        <tr class="border-b border-gray-100 hover:bg-gray-50 transition-colors">
                            <td class="py-3 px-4">
                                <div class="font-medium text-gray-900">{{ company.name }}</div>
                            </td>
                            <td class="py-3 px-4">
                                <span class="px-3 py-1 bg-green-100 text-green-800 rounded-full text-sm">
                                    {{ company.sector ?: 'Non spécifié' }}
                                </span>
                            </td>
                            <td class="py-3 px-4 text-gray-600">{{ company.location }}</td>
                            <td class="py-3 px-4 text-gray-600">
                                {% if company.email %}
                                    <a href="mailto:{{ company.email }}" class="text-blue-600 hover:underline">{{ company.email }}</a>
                                {% else %}
                                    <span class="text-gray-400">Non spécifié</span>
                                {% endif %}
                            </td>
                            <td class="py-3 px-4 text-gray-600">
                                {{ company.phone ?: 'Non spécifié' }}
                            </td>
                            <td class="py-3 px-4 text-gray-600">
                                {{ company.created_at ? company.created_at|date('d/m/Y H:i') : 'N/A' }}
                            </td>
                            <td class="py-3 px-4">
                                <div class="flex justify-center gap-2">
                                    <form action="/companies/edit/{{ company.id }}" method="GET" style="display: inline;">
                                        <button type="submit" class="bg-blue-500 hover:bg-blue-600 text-white px-3 py-1 rounded text-sm transition-colors">
                                            ✏️ Modifier
                                        </button>
                                    </form>
                                    <form action="/companies/delete/{{ company.id }}" method="POST" style="display: inline;" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer l\'entreprise \"{{ company.name }}\"? Cette action est irréversible!')">
                                        <button type="submit" class="bg-red-500 hover:bg-red-600 text-white px-3 py-1 rounded text-sm transition-colors">
                                            🗑️ Supprimer
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    {% endif %}
</div>


{% endblock %}